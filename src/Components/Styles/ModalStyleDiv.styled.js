import styled from 'styled-components';

export const ModalStyleDiv = styled.div`
    padding: 10px;
    background: linear-gradient(90deg, #b9deed, #efefef);
    border: 1px solid #93aed2;
    button{
        padding: 16px;
        font-weight: 600;
        font-size: 15px;
        border-radius: 10px;
        margin: 20px;
        background-color: transparent;
        cursor: pointer;
    }
`